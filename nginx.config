server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /assets/ {
        # let nginx serve static assets normally
        try_files $uri =404;
    }
}
